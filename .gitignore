# Entorno virtual (se regenera con INSTALAR.bat)
venv/

# Python compilado
__pycache__/
*.py[cod]
*.so

# Archivos temporales de Office (bloqueos)
~$*.xlsx
~$*.xls

# Archivos temporales de SQLite WAL mode (nunca deben ir al repo)
*.db-shm
*.db-wal

# Basura del sistema operativo
.DS_Store
Thumbs.db
desktop.ini
passwords.txt
