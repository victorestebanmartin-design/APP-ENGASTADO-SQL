# =====================================================
# DEPENDENCIAS - APP ENGASTADO SQL  
# =====================================================
# Versión: 2.0-SQLite (sin permisos admin)
# Python: 3.9+

# Servidor WSGI para producción
# gunicorn >= 22 corrige CVE-2024-1135 y >= 23 CVE-2024-6827 (request smuggling)
gunicorn==23.0.0
waitress==3.0.2

# Framework Web
# Flask >= 3.1.1 corrige CVE-2025-47278 (fallback de claves de sesión)
# Jinja2 >= 3.1.6 corrige CVE-2024-56201 y CVE-2025-27516 (escape del sandbox)
Flask==3.1.1
Werkzeug==3.1.3
Jinja2==3.1.6
MarkupSafe==3.0.2
click==8.1.7
itsdangerous==2.2.0
blinker==1.9.0

# Base de datos SQLite (incluido en Python, no requiere instalación)
# sqlite3 viene con Python standard library
# 2.0.36+ requerido para Python 3.13 (fix TypingOnly __firstlineno__/__static_attributes__)
SQLAlchemy==2.0.50

# Procesamiento Excel
openpyxl==3.1.5
pandas==2.2.3
numpy==2.1.3
et-xmlfile==2.0.0

# Pantallas ESP32: deteccion de puertos USB y subida de firmware desde Admin
pyserial>=3.5
mpremote>=1.24
# esptool graba MicroPython en placas nuevas (mpremote solo habla con placas
# que YA lo tienen). Los comandos se invocan con guion bajo (erase_flash /
# write_flash), que funcionan tanto en esptool 4 como en 5.
esptool>=4.7

# Utilidades
python-dateutil==2.9.0.post0
pytz==2024.2
six==1.16.0
tzdata==2024.2
colorama==0.4.6

# Para desarrollo y testing: pip install -r requirements-dev.txt
# (incluye pytest; los tests están en tests/ y se lanzan con: pytest)

# NOTAS DE INSTALACIÓN:
# 1. SQLite viene incluido con Python (no requiere instalación externa)
# 2. NO requiere permisos de administrador
# 3. Base de datos se guarda en: data/engastado.db
#
# 4. Crear entorno virtual:
#    python -m venv venv
#    venv\Scripts\activate
#
# 5. Instalar dependencias:
#    pip install -r requirements.txt
#
# 6. Inicializar base de datos:
#    sqlite3 data/engastado.db < schema_sqlite.sql
#    O desde Python: run_sql.py lo hace automáticamente
