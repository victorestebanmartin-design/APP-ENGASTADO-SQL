# Dependencias de desarrollo (tests). Instalar con:
#   pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.4
truststore==0.10.4   # deploy.py: ssl usa el almacén de certificados de Windows
